export default
`package main
import "fmt"

func findRoots(a, b, c float64) (float64, float64) {
  return 0, 0
}

func main() {
  x1, x2 := findRoots(2, 10, 8)
  fmt.Println("Roots: %f, %f", x1, x2)
}`
